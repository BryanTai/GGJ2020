using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoodMonitor : MonoBehaviour
{
    public GameObject player;
    // Start is called before the first frame update
    void Start()
    {
        /*
         update the chat with has joined the game for each character
         */

        // variable that is the gui text box
    }

    // Update is called once per frame
    void Update()
    {

        // variable that is the 4 team mates

        /*
        for (teammate in teammates)
        {
            check mood
            if (mood > 75)
            {
                teammate.chat_Action_1()
            }
            if (mood == 0)
            {
                teammate.leave()
            }
        }
        */
    }
}
